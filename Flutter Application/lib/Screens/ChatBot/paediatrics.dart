import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/ChatBot/components/paeds_body.dart';

class Paediatrics extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Body(),
    );
  }
}
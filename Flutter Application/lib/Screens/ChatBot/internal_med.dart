import 'package:flutter/material.dart';
import 'package:flutter_auth/Screens/ChatBot/components/body.dart';

class InternalMedicine extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      //final EdgeInsets padding = MediaQu

      body: Body(),
    );
  }
}